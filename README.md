# nvim

My neovim configuration files using Lazy, Mason, and None-ls.

Built mainly for `python` and `c` programming.

All plugin keybinds are stored in `which-key.lua`, and neovim base keybinds are stored in `settings.lua`.
